# Keep the build context small and never copy deps, build output, or secrets
# into the image (deps are reinstalled fresh via `npm ci`).
node_modules
dist
dist-ssr
dev-dist
.env
.env.*
!.env.example
*.log
.git
.gitignore
Dockerfile
.dockerignore
coverage
.vscode
.idea
.DS_Store
